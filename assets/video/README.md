Drop the hero background video here once Laura sends it:

- `hero.mp4` — short, looping, no audio needed (it plays muted). Landscape orientation works best since it fills a wide hero section.
- `hero-poster.jpg` — a still frame from the video, shown while it loads (and on very slow connections).

No code changes needed — `index.html` already points at these two files.
